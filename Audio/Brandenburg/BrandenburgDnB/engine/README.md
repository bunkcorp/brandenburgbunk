# Engine

- **SampleLoader.scd** – `~loadBrandenburg(path, bpm, numSlices)`, `~getSliceFrames(sliceIndex)`.
- **SliceSampler.scd** – `\brandenburg_slice` SynthDef: bufnum, startFrame, numFrames, speed, reverse, outBus.
